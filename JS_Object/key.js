//** Object Keys */
var object1 ={
    language: 'JavaScript',
    founder: 'Brenddan Erich',
    estd: 1995,
    isPublick: true,
    uses: ['web', 'server', 'desktop']
}

// uses object keys
const keys = Object.keys(object1);
//? uses object Values
var values = Object.values(object1);
// uses object Entries(key+valuse)
var Entries = Object.entries(object1);
//console.log(keys);
//console.log(values);
console.log(Entries);

//! Object ShortHand 
